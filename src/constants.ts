export const api = {
    rooms: '/api/rooms',
    bookings: '/api/bookings'
}
export const URL_DATE_FORMAT = "DD/MM/YYYY";
export const URL_TIME_FORMAT = "HH:mm";

// time to live in minutes for fetch cache
export const fetchCacheTTL = 30

export const URL_KEYS = {
    CAPACITY: 'capacity',
    START_DATE_TIME: 'from',
    START_TIME: 'startTime',
    END_TIME:'endTime',
    ROOM: 'room',
}